//Guardar en un array llamado valores, los números del 1 al 5.

//const valores = [1, 2, 3, 4, 5];

function agregar(){
    let valores = [];
    for(i = 0; i <= 5 ; i++){
        valores = i;
        console.log(valores);
    }
}
agregar();